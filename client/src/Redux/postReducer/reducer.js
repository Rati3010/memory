import * as types from "./actionType"
const reducer = (state=[], action) => {
    switch (action.type) {
        case value:
            return state
        case value:
            return state
        default:
            return state;
    }
}

export default reducer